## Packages
framer-motion | Essential for the bouncy, cute animations and the reveal effect
canvas-confetti | Provides the satisfying celebratory burst on the monthly reveal
@types/canvas-confetti | Type definitions for canvas-confetti
clsx | For conditional class merging (already in base stack, just confirming usage)
tailwind-merge | For conditional class merging (already in base stack)

## Notes
User identity (David vs Arlayna) is managed via local React state/Context since full auth isn't needed.
Dynamic images are not provided; we will rely on UI illustration (Globe Mascot) and user-provided URLs for cause photos.
